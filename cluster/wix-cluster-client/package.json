{
  "name": "wix-cluster-client",
  "version": "0.0.1",
  "description": "wix cluster client library",
  "main": "index.js",
  "license": "ISC",
  "author": "vilius@wix.com",
  "homepage": "https://github.com/wix/server-platform-js/cluster/wix-cluster-client",
  "scripts": {
    "build": ":",
    "lint": "eslint lib/ test/*.js",
    "pretest": "npm run lint -s",
    "test": "mocha --reporter mocha-env-reporter",
    "release": "wnpm-release"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wix/server-platform-js/cluster/wix-cluster-client"
  },
  "keywords": [
    "wix",
    "cluster"
  ],
  "publishConfig": {
    "registry": "http://repo.dev.wix/artifactory/api/npm/npm-local/"
  },
  "dependencies": {
    "wix-cluster": "latest",
    "wix-childprocess-testkit": "latest"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^2.5.3",
    "eslint-config-spjs": "latest",
    "eslint-plugin-mocha": "^2.0.0",
    "express": "^4.13.4",
    "mocha": "^2.4.5",
    "mocha-env-reporter": "latest",
    "node-fetch": "^1.4.1",
    "wnpm-ci": "latest"
  }
}
