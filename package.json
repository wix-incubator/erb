{
  "name": "server-platform-js",
  "version": "1.0.0",
  "description": "Mono-repo for wixy node libraries for 'node as a front-end server'.",
  "homepage": "https://github.com/wix-private/server-platform-js",  
  "private": true,
  "scripts": {
    "postinstall": "octo init",
    "bootstrap": "octo bootstrap -n",
    "sync": "octo deps sync --save && octo modules sync --save",
    "idea": "octo idea",
    "test": "octo run build test",
    "prebuild-all": "octo modules sync && octo deps sync", 
    "build-all": "octo modules unbuild && octo bootstrap -n && octo run build test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wix-private/server-platform-js.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wix-private/server-platform-js/issues"
  },
  "devDependencies": {
    "octopus-cli": "latest"
  }
}
